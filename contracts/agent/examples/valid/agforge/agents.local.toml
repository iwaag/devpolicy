# Valid overlay: machine facts and secret references only.
# Paths are illustrative placeholders, not committed machine facts.
schema = "ag.agent-config.v1"

[local.harness.claude_code]
command_glob = "~/.vscode-server/extensions/anthropic.claude-code-*/resources/native-binary/claude"

[local.provider.ollama]
base_url = "http://ollama-host.example:11434"

[local.secrets]
anthropic_api_key_file = "~/.secrets/anthropic_api_key"
