# EXPECT: E_OVERLAY_SCOPE
# This file is an OVERLAY (agents.local.toml) example. Overlays may not
# define new profiles or models — only local facts and role→profile
# selection among committed profiles.
schema = "ag.agent-config.v1"

[local.harness.agcode]
command = "~/.local/bin/python3"

[profiles.sneaky]
harness = "agcode"
model = "ollama/qwen3.6:35b-a3b-coding-nvfp4"

[roles.front]
profile = "sneaky"
