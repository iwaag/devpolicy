# EXPECT: E_SECRET_VALUE
# This file is an OVERLAY (agents.local.toml) example. Secret keys must
# be references (*_file or *_env), never literal values. The value below
# is a fake for the example.
schema = "ag.agent-config.v1"

[local.secrets]
anthropic_api_key = "sk-ant-EXAMPLE-NOT-A-REAL-KEY"
